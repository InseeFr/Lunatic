import Datepicker from './datepicker';
import { createLunaticComponent } from '../commons';
import './datepicker.scss';

export default createLunaticComponent(Datepicker, {
	inputId: 'lunatic-datepicker',
});
