# CheckboxGroup

Affiche une liste de case à cocher. Chaque case sera associé à une variable spécifique précisé au travers de l'option **responses**. Chaque réponse prendra la valeur `true` ou `false` en fonction de l'état de la case à cocher.

import {GoToType} from '@site/src/components/GoToType';

<GoToType type="ComponentCheckboxGroupType"/>

import source from '../../../../src/stories/checkbox-group/source.json';
import {FormExample} from '@site/src/components/FormExample/FormExample';

<FormExample source={source} />
